# CLAUDE.md — Auto-École Marti Project Memory

> Read this file at the start of every session. It contains everything known about this project.

---

## Project Overview

**Auto-École Marti** is a French driving school website based in Bayonne (64100), operating since 1962.
- Domain: `autoecolemarti.fr` (hosted on **Cloudflare Pages**, deployed from this GitHub repo; domain configured in the Cloudflare dashboard — no `CNAME` file in the repo)
- SIRET: 382 930 188 00018
- Address: 13 rue Marengo, Bayonne 64100
- Google Analytics: G-DEK5H3Z9DR

---

## Folder Structure

```
/autoecolemarti/
├── index.html                    # Homepage
├── faq.html                      # FAQ page — 14 Q&A with JSON-LD FAQPage schema
├── reservation.html              # Booking form (Supabase)
├── merci.html                    # Thank-you page after booking (6s countdown redirect)
├── devis.html                    # Devis estimateur interactif (noindex, orphelin volontairement — à lancer)
├── mentions-legales.html         # Legal notices (uses Inter font, not DM Sans)
├── espace-eleves.html            # Student resources page (post-enrollment: code platform, app, PDFs, contacts)
├── auto-ecole-anglet.html        # Local SEO landing page — Anglet (RDV Place Quintaou); self-contained inline CSS
├── auto-ecole-biarritz.html      # Local SEO landing page — Biarritz (départ Anglet/Bayonne au choix); self-contained inline CSS
├── 404.html                      # Custom 404 page (noindex, standalone inline CSS, links to key pages)
├── og-image.png                  # Social sharing image 1200×630 (og:image + twitter:image on all indexable pages)
├── robots.txt                    # SEO directives
├── sitemap.xml                   # SEO sitemap (auto-generated by GitHub Actions)
├── llms.txt                      # LLM-friendly site content for AI indexing
├── _redirects                    # 36 règles 301 explicites (canonicalise /<page>.html → /<page>) — généré manuellement
├── favicon.ico                   # Brand icon
├── favicon.ico.png               # Brand icon (PNG version)
├── STYLE_GUIDE.md                # Design system documentation
├── CLAUDE.md                     # This file
├── ROADMAP.md                    # Backlog des développements futurs souhaités
├── TARIFS.md                     # Référentiel des tarifs — recense TOUTES les occurrences de prix du site
├── README.md                     # GitHub repository README
├── package.json                  # Node.js config: `npm run sitemap`
│
├── css/
│   ├── fonts.css                 # Self-hosted font declarations (@font-face) — DM Sans, Playfair, Inter
│   ├── fonts-legal.css           # Font declarations for legal page only (Inter + DM Serif Display)
│   ├── base.css                  # Shell commun partagé (reset, :root cœur, navbar desktop + dropdown + hamburger, footer, skip-link/focus) — chargé par les pages formules/* et pages locales
│   ├── nav-mobile.css            # Menu hamburger mobile partagé (toutes pages sauf index.html) — à charger APRÈS le <style> inline
│   ├── index.css                 # Global styles: navbar, hero, cards, footer
│   ├── blog-index.css            # Blog listing: hero, filters, article cards
│   ├── blog-article.css          # Blog article: hero, breadcrumbs, content, CTAs
│   └── tarteaucitron-custom.css  # RGPD cookie banner custom styles (Basque brand colors)
│
├── fonts/                        # Self-hosted WOFF2 font files (540 KB — in .claudeignore)
│   ├── DM Sans (weights 300–700, Latin + Latin-ext)
│   ├── Playfair Display (weights 400, 600, 700)
│   ├── Inter (weights 300, 400, 500)
│   └── DM Serif Display (weight 400 normal + italic)
│
├── formules/                     # Course/pricing pages
│   ├── index.html                # Hub interactif: quiz 3 étapes → recommandation personnalisée
│   ├── conduite-accompagnee.html # AAC supervised driving, age 15+, 1 629 € TTC
│   ├── permis-b-manuel.html      # Manual transmission license
│   └── permis-b-automatique.html # Automatic transmission license
│
├── blog/                         # 21 articles + index
│   ├── index.html                # Blog hub with category filters
│   ├── aac-apprentissage-anticipe-conduite.html
│   ├── angles-morts.html
│   ├── auto-ecole-en-ligne-ou-marti.html
│   ├── boite-automatique-ou-manuelle.html
│   ├── combien-coute-permis-conduire-bayonne.html
│   ├── comment-reussir-son-permis-conduire.html
│   ├── conduire-sur-le-bab-guide-complet.html
│   ├── conduite-supervisee.html
│   ├── dangers-alcool.html
│   ├── distances-securite.html
│   ├── fatigue-volant.html
│   ├── infractions-code-route.html
│   ├── permis-apres-30-40-50-ans.html
│   ├── questions-pieges-code-route.html
│   ├── quiz-sanctions.html
│   ├── reflexes-quiz.html
│   ├── reflexes-urgence-conduite.html
│   ├── regles-permis-probatoire.html
│   ├── reussir-code-route-premier-coup.html
│   └── simulateur-ecoconduite.html
│
├── quizz/
│   ├── quizz_panneaux.html       # Interactive road sign quiz (200+ signs)
│   ├── panneaux_data.js          # 51 KB static data file (in .claudeignore)
│   └── panneaux/                 # 294 SVG road sign files, 2.7 MB (in .claudeignore)
│
├── plaquettes/                   # Brochures PDF — referenced in formules pages
│   ├── Plaquette_AAC.pdf
│   ├── Plaquette_Permis_B_BVA.pdf
│   └── Plaquette_Permis_B_Manuel.pdf
│
├── scripts/
│   ├── generate-sitemap.js       # Node.js sitemap generator (189 lines, `npm run sitemap`)
│   ├── consent.js                # tarteaucitron RGPD init (29 lines)
│   ├── load-consent.js           # Lazy-loader: injecte tarteaucitron.js → fr.js → consent.js à l'inactivité/1re interaction; expose window.loadConsent(cb)
│   ├── email-decoder.js          # Décodeur partagé des liens email obfusqués (mutualise le bloc inline)
│   ├── nav.js                    # Menu hamburger mobile partagé (toutes pages sauf index.html)
│   ├── article.js                # Logique partagée des articles blog (TOC, bouton scroll-to-top…)
│   └── add_tarteaucitron.py      # Python batch script — adds cookie banner to HTML files
│
├── vendor/
│   └── tarteaucitron/            # RGPD/cookie consent library (CNIL-compliant)
│       ├── tarteaucitron.js
│       ├── tarteaucitron.css
│       ├── tarteaucitron.services.js
│       └── lang/tarteaucitron.fr.js
│
└── .github/
    └── workflows/
        └── generate-sitemap.yml  # CI/CD: auto-regenerates sitemap.xml on HTML file push
```

---

## Design System

### Color Palette (Basque Identity)

| Role             | Name          | Hex       | CSS Variable       | Usage                          |
|------------------|---------------|-----------|--------------------|--------------------------------|
| Primary Brand    | Basque Red    | `#d63031` | `--basque-rouge`   | CTAs, buttons, primary accents |
| Secondary Brand  | Basque Teal   | `#00695c` | `--basque-vert`    | Secondary CTAs, success states |
| Accent           | Gold          | `#d4a853` | `--or`             | Premium highlights, tariffs    |
| Background       | Off-white     | `#fafaf8` | `--blanc`          | Primary page background        |
| Secondary BG     | Crème         | `#f5f0e8` | `--creme`          | Cards, alternate sections      |
| Featured Card BG | Navy          | `#1e2a4a` | *(inline)*         | Featured pricing card          |
| Body Text        | Near-black    | `#0a0a0a` | `--noir`           | All body text                  |
| Secondary Text   | Grey          | `#6b7280` | `--gris`           | Metadata, descriptions         |
| Borders          | Light Grey    | `#e5e5e5` | `--gris-clair`     | Dividers, subtle borders       |

### CSS Custom Properties — Complete Reference

All `:root` variables in use across the codebase:

```css
:root {
  /* Brand colors */
  --basque-rouge: #d63031;
  --basque-vert: #00695c;
  --or: #d4a853;

  /* Neutrals */
  --noir: #0a0a0a;
  --blanc: #fafaf8;
  --creme: #f5f0e8;
  --gris: #6b7280;
  --gris-clair: #e5e5e5;

  /* Legacy aliases (also used) */
  --rouge: #c0392b;
  --rouge-doux: #e74c3c;
  --rouge-sombre: #922b21;
  --vert: #1a5c38;
  --vert-clair: #27ae60;

  /* Shadows */
  --ombre: 0 4px 30px rgba(0,0,0,0.08);
  --ombre-forte: 0 20px 60px rgba(0,0,0,0.12);

  /* Shape */
  --radius: 16px;
  --radius-sm: 10px;

  /* Motion */
  --transition: 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
```

**Blog article-specific variables** (overridden per article in `<style>` or inline):

```css
:root {
  --article-accent: var(--basque-vert);
  --article-accent-bg: rgba(0,105,92,0.06);
  --article-hero-gradient: linear-gradient(135deg, #f0fffe 0%, #c8f0ec 50%, #a8e4df 100%);
  --cta-gradient: linear-gradient(135deg, var(--basque-vert) 0%, #004d44 100%);
}
```

### Typography

- **H1, H2 (hero headings)**: Playfair Display, serif, italic for accent words
- **Body, Nav, UI, buttons**: DM Sans, sans-serif
- **Legal page only**: Inter + DM Serif Display (loaded from `css/fonts-legal.css`)
- **All fonts are self-hosted** (WOFF2, Latin + Latin-ext) — RGPD-compliant, no Google Fonts CDN

Fluid type scale uses `clamp()`:
- H1 (homepage): `clamp(2.8rem, 5vw, 4.2rem)`
- H1 (blog): `clamp(2.4rem, 5vw, 3.8rem)`
- H2 (sections): `clamp(2rem, 3.5vw, 2.8rem)`

### Layout & Spacing

- Container max-width: **1200px** (global), **780px** (articles)
- Section padding: `6rem 2rem` desktop → `4rem 1.2rem` mobile
- Border-radius: `16px` standard (`--radius`), `10px` small (`--radius-sm`), `50px` pills (buttons)
- Grid gaps: `1.5rem` standard
- Responsive breakpoints: `1024px` (tablet), `768px` (mobile)

### Buttons

- **Primary** `.btn-primary`: Red `#d63031` background, white text, pill shape, `box-shadow: 0 4px 20px rgba(red)`
- **Secondary** `.btn-secondary`: Transparent, dark border
- **Nav CTA** `.nav-cta`: Smaller pill, red background
- Padding: `1rem 2rem`
- Hover: `translateY(-2px)` + shadow increase

### Animations

- Global easing: `cubic-bezier(0.25, 0.46, 0.45, 0.94)` over `0.4s` (`--transition`)
- Card hover: `translateY(-6px)` to `translateY(-8px)` + shadow increase
- Hero entrance: `@keyframes fadeInUp` (opacity + translateY), staggered delays 0.1–0.4s
- Scroll animations: `IntersectionObserver`, `.fade-in` → `.fade-in.visible`
- Badge pulse: scale oscillation

---

## Pages & Sections

### Homepage (`index.html`)
1. **Navbar** — Fixed, transparent → blurred white on scroll (`nav.scrolled`), hamburger on mobile, formations dropdown menu
2. **Hero** — 88vh, 2-col grid (text left, hero-card right), decorative color orbs (pseudo-elements)
3. **Info bar** — 4 stats cards: students trained, success rate, years, affiliations
4. **Formations** — 3 pricing tiers (featured card in dark navy + gold, "POPULAIRE" badge)
5. **Tarifs/hourly rates** — Price per lesson hour
6. **File preparation** — Overlay/modal for required documents (`.dossier-modal`)
7. **History/about** — "Since 1962" narrative section
8. **Testimonials** — Student reviews
9. **Contact/reservation** — CTA to booking form
10. **Footer** — Links, contact details, legal

### FAQ (`faq.html`)
- 14 Q&A items covering: hours required, manual vs. auto, pricing, AAC, documents, payment plans, exam format, probationary period, supervised driving, lesson locations
- JSON-LD FAQPage schema for SEO rich results
- Canonical URL, Open Graph meta tags
- Sticky navbar with simplified nav-logo

### Formations Hub (`formules/index.html`)
- Interactive 3-step quiz to recommend the right course:
  - Step 1: Age (15+ or 18+)
  - Step 2: Is there an accompanying adult? (for AAC)
  - Step 3: Confidence with manual gearbox?
- Slide animations between questions (left/right direction)
- Progress bar with step counter
- Personalized result card + related articles
- **⚠️ Uses inline CSS** (no separate stylesheet — exception to the rule)
- JSON-LD BreadcrumbList schema

### Formules Detail Pages (`formules/conduite-accompagnee.html`, `permis-b-manuel.html`, `permis-b-automatique.html`)
- Hero with price + age requirement
- Advantages grid
- Itemized tariff breakdown
- Step-by-step program
- Prerequisites & conditions
- PDF lightbox viewer (PDF.js 3.11.174 via CDN) — linked to plaquettes PDFs
- Booking CTA
- FAQs

### Blog (`blog/index.html` + 21 articles)
- Blog hub has featured article + 21 article cards with category filters (dont 1 carte « bientôt disponible »)
- **Category filters**: Code de la route, Conduite, AAC, Tarifs & financement, Réglementation, Conseils pratiques, Outils pédagogiques
- Each article has: category-colored hero gradient, breadcrumbs, reading time, callout boxes, related articles, CTA banner
- Hero height: `52vh` (vs. `88vh` on homepage)

**Blog articles list (21 total):**
| File | Topic | Category |
|------|-------|----------|
| `aac-apprentissage-anticipe-conduite.html` | AAC overview | Conduite accompagnée |
| `angles-morts.html` | Blind spots | Conduite |
| `auto-ecole-en-ligne-ou-marti.html` | Online vs. in-person | Conseils pratiques |
| `boite-automatique-ou-manuelle.html` | Auto vs. manual | Conseils pratiques |
| `combien-coute-permis-conduire-bayonne.html` | Pricing guide Bayonne | Tarifs & financement |
| `comment-reussir-son-permis-conduire.html` | Exam success guide | Conseils pratiques |
| `conduire-sur-le-bab-guide-complet.html` | Driving on the BAB | Conduite |
| `conduite-supervisee.html` | Post-training supervised driving | Conduite |
| `dangers-alcool.html` | Alcohol effects & sanctions | Réglementation |
| `distances-securite.html` | Interactive safety distance calc | Outils pédagogiques |
| `fatigue-volant.html` | Driver fatigue | Conseils pratiques |
| `infractions-code-route.html` | Infraction fine/point reference | Réglementation |
| `permis-apres-30-40-50-ans.html` | Getting license as an adult | Conseils pratiques |
| `permis-lyceen-etudiant-bayonne.html` | License for high-school/student (Bayonne) | Conseils pratiques |
| `questions-pieges-code-route.html` | Tricky exam questions | Code de la route |
| `quiz-sanctions.html` | Sanctions quiz | Code de la route |
| `reflexes-quiz.html` | Reflexes quiz | Outils pédagogiques |
| `reflexes-urgence-conduite.html` | Emergency response quiz | Outils pédagogiques |
| `regles-permis-probatoire.html` | Probationary license rules | Réglementation |
| `reussir-code-route-premier-coup.html` | Code exam strategy | Code de la route |
| `simulateur-ecoconduite.html` | Eco-driving simulator | Outils pédagogiques |

### Quiz (`quizz/quizz_panneaux.html`)
- Interactive road sign identification
- 200+ official French signs (CEREMA/IISR standard)
- Categories: danger, priorité, interdiction, obligation, information, service, temporaire
- 10 questions per session, randomized with Fisher-Yates shuffle
- Real-time feedback on answers, final score screen
- Category filter bar (dynamically generated)

### Reservation (`reservation.html`)
- Supabase real-time integration (via CDN `@supabase/supabase-js@2`)
- Connection status indicator (red/green dot — bottom left)
- Tabs for different reservation types
- Calendar grid with slot states: available (green), booked (red), pending (yellow), selected (primary red)
- Modal for slot details
- Form validation + RGPD consent checkbox

### Legal (`mentions-legales.html`)
- Uses Inter + DM Serif Display fonts (`css/fonts-legal.css`), not DM Sans
- SIRET, address, contact, legal disclaimers
- Section `#cookies` for cookie policy (referenced by tarteaucitron)

### Espace Élèves (`espace-eleves.html`)
- Public teaser page for post-enrollment student resources
- Green hero gradient (same as AAC formule page)
- 3-step "how it works" section (inscription → accès → révision)
- 4 resource cards: code platform (placeholder), mobile app (placeholder store buttons), PDF brochures (3 direct links), contacts
- Resource cards with `.access-note` badge for links communicated at enrollment
- CTA section linking to `formules/` and `reservation.html`
- Linked from the footer "Navigation" column of index, faq, blog and formules hubs (no navbar link yet)
- Canonical URL without `.html`: `https://autoecolemarti.fr/espace-eleves`
- Sitemap priority: 0.8

### Pages locales SEO (`auto-ecole-anglet.html`, `auto-ecole-biarritz.html`)
- Landing pages locales **auto-contenues** (CSS inline, calquées sur les pages `formules/`), ciblant
  « auto-école / permis Anglet » et « auto-école / permis Biarritz ».
- Hero vert + sections : atout local, « conduire à … », formules (3 cartes), mini-FAQ (`<details>`, sans JS), CTA.
- JSON-LD : `DrivingSchool`/`LocalBusiness` (`areaServed` recentré sur la ville), `BreadcrumbList`, `FAQPage`.
- Faits : adresse Bayonne (13 rue Marengo), RDV Place Quintaou (Anglet) ; Biarritz = départ Anglet **ou** Bayonne au choix ; tarifs identiques. **Aucun lien vers `reservation.html`.**
- Maillées depuis l'article BAB (`blog/conduire-sur-le-bab-guide-complet.html`), les footers des pages principales (homepage, FAQ, blog hub, formules hub + 3 pages détail, espace élèves), la FAQ « Où se déroulent les leçons ? » (lien Quintaou → Anglet), et entre elles. Canonical sans `.html`. Sitemap priority 0.8.

---

## Technical Stack

- **Pure HTML/CSS/JS** — No frontend framework
- **Cloudflare Pages** — Hosting/deploy (connected to this GitHub repo; auto-deploys on push). Serves clean URLs natively and **auto-redirects** `/<page>.html` → `/<page>` and `/index.html` → `/` with a **308 (permanent)** redirect. En complément, un fichier `_redirects` (36 règles **301** explicites, généré manuellement) canonicalise également les URLs `.html`. Custom domain + `www` → apex + "Always Use HTTPS" are managed in the Cloudflare dashboard, not in the repo (no `CNAME` file). `404.html` at the repo root is served automatically as the not-found page.
- **Supabase** — Real-time backend for booking (`reservation.html` only)
- **PDF.js 3.11.174** — Document lightbox viewer on all formules detail pages (CDN worker)
- **tarteaucitron.js** — CNIL-compliant RGPD cookie consent (local `/vendor/tarteaucitron/`)
- **Google Analytics** — Tag ID: `G-DEK5H3Z9DR` (loaded via tarteaucitron consent)
- **IntersectionObserver** — Scroll-triggered `.fade-in` animations
- **SVG** — All road signs in `quizz/panneaux/`
- **Node.js** — Sitemap generator (`npm run sitemap` → `scripts/generate-sitemap.js`)
- **GitHub Actions** — Auto-regenerates `sitemap.xml` on HTML push (`.github/workflows/generate-sitemap.yml`)
- **Python** — Batch script `scripts/add_tarteaucitron.py` for adding RGPD banner to HTML files

---

## JavaScript Patterns (Reusable)

### Navbar scroll effect (all pages)
```javascript
window.addEventListener('scroll', () => {
  nav.classList.toggle('scrolled', window.scrollY > 50);
});
// CSS: nav.scrolled { background: rgba(255,255,255,0.92); backdrop-filter: blur(20px); }
```

### Fade-in on scroll (IntersectionObserver)
```javascript
const observer = new IntersectionObserver((entries) => {
  entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); });
}, { threshold: 0.15 });
document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));
// CSS: .fade-in { opacity: 0; transform: translateY(30px); }
//      .fade-in.visible { opacity: 1; transform: translateY(0); transition: 0.8s ease; }
```

### Email obfuscation (all contact links)
```html
<!-- HTML: use HTML entities for email address -->
<a data-email="&#97;&#117;&#116;&#111;..." data-subject="Sujet" data-show-email="true">...</a>
```
```javascript
// JS (at bottom of each page):
document.querySelectorAll('[data-email]').forEach(function(el) {
  var h = 'mailto:' + el.dataset.email;
  if (el.dataset.subject) h += '?subject=' + el.dataset.subject;
  el.href = h;
  if (el.dataset.showEmail) el.textContent = el.dataset.email;
});
```

### PDF.js lightbox (formules detail pages)
```javascript
// Functions: openLightbox(), closeLightbox(), renderPage(num), nextPage(), prevPage()
//            zoomIn(), zoomOut()
// Key vars: pdfDoc, currentPage, scale (default 1.2)
// Download: fetch PDF → Blob → temporary <a> click
// Close: Escape key + click overlay
```

### Branched quiz (formules/index.html)
```javascript
// Key functions: goTo(targetId, dir), showResult(type), restart()
// State: history[], currentStep, activeScreen
// Animation: slide left/right CSS classes on question card
// Auto-calibrate height: calibrateQuizHeight() measures all screens → sets min-height
```

### Countdown redirect (merci.html)
```javascript
let seconds = 6;
const interval = setInterval(() => {
  seconds--;
  countdownEl.textContent = seconds;
  if (seconds <= 0) { clearInterval(interval); window.location.href = '/'; }
}, 1000);
// CSS: .progress-bar { animation: shrink 6s linear forwards; }
//      @keyframes shrink { from { width: 100%; } to { width: 0%; } }
```

---

## RGPD / Cookie Consent (tarteaucitron)

All pages include the tarteaucitron cookie consent banner for CNIL compliance.

### Files involved
- `/scripts/load-consent.js` — **Lazy-loader chargé sur chaque page** : injecte la lib RGPD **et son CSS** à l'inactivité (`requestIdleCallback`) ou à la 1re interaction (scroll/clic/touch), au lieu de bloquer le chargement initial. Expose `window.loadConsent(cb)` pour les déclencheurs explicites (ex. bouton « Gérer mes cookies » des mentions légales).
- `/vendor/tarteaucitron/tarteaucitron.css` + `/css/tarteaucitron-custom.css` — Feuilles de style du bandeau, **injectées par load-consent.js** au moment du lazy-load (vendor puis override de marque). **Plus jamais** en `<link>` render-blocking dans le `<head>` : elles ne servent qu'à l'affichage différé du bandeau.
- `/vendor/tarteaucitron/tarteaucitron.js` — Main library (injectée par load-consent.js)
- `/vendor/tarteaucitron/tarteaucitron.services.js` — Service definitions (Google Analytics, etc.) — chargée à la volée par tarteaucitron au besoin, jamais en statique
- `/vendor/tarteaucitron/lang/tarteaucitron.fr.js` — French translations
- `/scripts/consent.js` — Project initialization (loads after tarteaucitron.js)

### Configuration (consent.js)
```javascript
tarteaucitron.init({
  "privacyUrl": "/mentions-legales#cookies",
  "hashtag": "#tarteaucitron",
  "cookieName": "tarteaucitron",
  "highPrivacy": true,       // CNIL strict mode
  "handleBrowserDNTRequest": false,
  "removeCredit": true,
});
// Google Analytics integration:
tarteaucitron.user.analyticsUa = 'G-DEK5H3Z9DR';
(tarteaucitron.job = tarteaucitron.job || []).push('analytics');
```

### Adding the banner to new pages
Inclure **uniquement** le lazy-loader dans le `<head>` :
```html
<script defer src="/scripts/load-consent.js"></script>
```
`load-consent.js` se charge d'injecter, de façon différée et dans le bon ordre, les feuilles de style (`tarteaucitron.css` → `tarteaucitron-custom.css`) puis `tarteaucitron.js` → `lang/tarteaucitron.fr.js` → `consent.js`. **Ne plus** inclure ces scripts **ni les `<link>` CSS** en direct dans le `<head>` (cela bloquerait le rendu).

---

## Sitemap Generation

```bash
# Regenerate sitemap manually:
npm run sitemap

# Or trigger via GitHub Actions (push any .html file → workflow runs automatically)
```

**Sitemap priorities** (set in `scripts/generate-sitemap.js`):
- Homepage: 1.0
- Formules + FAQ: 0.9
- Blog hub + Quiz + Espace élèves: 0.8
- Pages locales (auto-ecole-anglet, auto-ecole-biarritz): 0.8
- Blog articles: 0.7

**Excluded from sitemap**: `merci.html`, `mentions-legales.html`, `reservation.html`, `404.html`, `quizz/panneaux/*`

---

## Key Conventions

- **⚠️ Tarifs — `TARIFS.md` fait autorité** : les prix sont écrits en dur dans **18 fichiers**
  (contenu visible, `<meta>`, JSON-LD `Offer`/`Course`, `DEVIS_CONFIG` de `devis.html`) sous
  9 formats différents (`967€`, `967&nbsp;€`, `"967"`, `1261€` sans espace, `920,00 €`…).
  **Lire `TARIFS.md` avant toute modification de prix** (inventaire ligne à ligne, checklist,
  pièges, montants à ne pas toucher : amendes, prix concurrents, tarifs d'État), **et le mettre
  à jour après**. Ne jamais faire un rechercher/remplacer naïf sur un montant.
- **All fonts self-hosted** — never use Google Fonts CDN (RGPD compliance)
- **Blog article hero backgrounds** use per-category CSS variables (`--article-accent`, `--article-hero-gradient`)
- **Callout boxes** in articles are color-coded: `.callout-info` (green), `.callout-warning` (gold), `.callout-success` (light green), `.callout-danger` (red), left 4px border
- **Featured pricing card**: navy `#1e2a4a` + gold `#d4a853` accents + "POPULAIRE" badge
- **Blog hero heights**: `52vh` (shorter than homepage at `88vh`)
- **formules/index.html** uses **inline CSS** (no external stylesheet) — this is an intentional exception
- **Shell CSS partagé (`css/base.css`)** : les 6 pages de la famille « formules détail + pages locales » (`auto-ecole-anglet`, `auto-ecole-biarritz`, `espace-eleves`, `formules/conduite-accompagnee`, `formules/permis-b-manuel`, `formules/permis-b-automatique`) chargent `css/base.css` (reset, `:root` cœur, navbar desktop, footer, skip-link/focus) et ne gardent en `<style>` inline que le **spécifique page** (hero, sections, cartes, boutons) + leurs **variables `:root` propres** (`--vert`, `--page-accent`, `--aac`…) + d'éventuels **overrides d'accent nav** (ex. `nav .nav-links a:hover` en `--aac`/`--page-accent`). Ordre impératif : `fonts.css` → `base.css` → `<style>` inline → `nav-mobile.css`. Les autres pages (`index`, `faq`, `404`, `merci`, `reservation`, `mentions-legales`, `formules/index`, quiz, blog) restent **volontairement autonomes** (systèmes de design distincts) — ne pas leur imposer `base.css`.
- **Email links** always use `data-email` HTML entity obfuscation + JS decoding (no raw `mailto:` in HTML)
- **Internal links use clean (extensionless) URLs** matching the canonicals/sitemap — never link to `*.html`. Use root-relative paths: `/` (homepage), `/faq`, `/formules/`, `/formules/conduite-accompagnee`, `/blog/`, `/blog/<slug>`, `/quizz/quizz_panneaux`, `/mentions-legales`, etc. Full absolute URLs (JSON-LD `@id`/`url`, JS redirects) also stay extensionless (`https://autoecolemarti.fr/blog/<slug>`). This consolidates SEO signals — Cloudflare Pages **308-redirects** `/<page>.html` → `/<page>` (permanent), so linking the clean form directly avoids a needless redirect hop and keeps all link equity on the canonical URL. Le fichier `_redirects` (301) couvre les mêmes paires au cas où (indépendant du mécanisme natif).
- **JSON-LD schemas** in use: `FAQPage` + `BreadcrumbList` (faq.html), `BreadcrumbList` (formules, articles, blog hub, quiz), `BlogPosting` with `datePublished` + `dateModified` + `mainEntityOfPage` (articles — exactly ONE article-type block per page, in the `<head>`), `Course` with `hasCourseInstance` (formules detail), `ItemList` (formules hub), `CollectionPage` (blog hub), `Quiz`/`LearningResource` (quizz), `LocalBusiness`/`DrivingSchool` + `Review`/`AggregateRating` + `sameAs` (homepage). Phone in JSON-LD is always E.164: `+33559591260`.
- **`mainEntityOfPage` se place au niveau du `BlogPosting`** — jamais dans `publisher` — avec `@id` = URL canonique de l'article (identique à `BlogPosting.url` et au `<link rel="canonical">`). Format : `"mainEntityOfPage": { "@type": "WebPage", "@id": "https://autoecolemarti.fr/blog/<slug>" }`.
- **Graphe d'entité — `@id` unique `https://autoecolemarti.fr/#business`** : toute référence à l'auto-école porte cet identifiant, ce qui la consolide en **une seule entité** aux yeux des moteurs. Concerné : les 3 blocs `LocalBusiness`/`DrivingSchool` (`index.html`, `auto-ecole-anglet`, `auto-ecole-biarritz` — leur `url` vaut `https://autoecolemarti.fr`, l'URL de l'entité, pas celle de la page), les `publisher`/`author` des 21 articles + du hub blog, et les `provider` des 3 pages formules détail + du quiz. Seule la home porte le `mainEntityOfPage` de l'entité ; les pages locales n'en déclarent pas. Le bloc `WebSite` de la home porte `@id: …/#website` + `publisher: { "@id": …/#business" }`.
- **Social meta on every indexable page**: `og:image` → `/og-image.png` (1200×630), `og:locale=fr_FR`, `og:url` matching the canonical, plus Twitter Cards (`summary_large_image`). Add them to any new page. Blog articles also carry `og:type=article` + `article:published_time`/`article:modified_time` (kept in sync with the JSON-LD dates).
- **Noindex pages have NO canonical**: `merci`, `devis`, `reservation`, `mentions-legales` carry `noindex, nofollow` and deliberately no `<link rel="canonical">` (conflicting signals); `404.html` is `noindex, follow`.
- **Font preloads**: pages preload `dm-sans-latin-400-normal.woff2` + `playfair-display-latin-400-normal.woff2` (body + heading fonts) in the `<head>`.
- **Article dates**: visible publication date is wrapped in `<time datetime="YYYY-MM-DD">`; update `dateModified` in the JSON-LD block AND `article:modified_time` when meaningfully editing an article
- **Navbar dropdown**: Formations menu has a hover dropdown with ARIA attributes (`aria-haspopup`, `aria-expanded`)
- **Design reference**: always check `STYLE_GUIDE.md` for detailed component specs

---

## Claude Code Integration & .claudeignore

**⚠️ Important for future debugging:** This project uses a `.claudeignore` file to optimize Claude Code performance.

### Why .claudeignore?

The following large/binary files are **intentionally ignored** to prevent timeouts and context bloat:

| Ignored Path | Size | Reason | Impact |
|---|---|---|---|
| `quizz/panneaux/` | 2.7 MB (294 SVG files) | Data files, never modified during development | Claude can't see these files |
| `fonts/` | 540 KB (WOFF2 binaries) | Binary assets, not touched during dev | Claude can't inspect these files |
| `quizz/panneaux_data.js` | 51 KB | Large static data file | Claude can't read this file |

**Total excluded:** ~3.3 MB (80% reduction in context size)

**Note on plaquettes PDFs:** `plaquettes/*.pdf` are NOT in `.claudeignore` — they are small (≈275 KB each) and referenced by name in formules pages. Claude cannot open binary PDFs but can see the file names.

### What This Means

- ✅ Claude **CAN still modify** `quizz/quizz_panneaux.html` (the quiz page itself)
- ✅ Claude **CAN modify** CSS, HTML, and JS files freely
- ❌ Claude **CANNOT see** the individual SVG files in `quizz/panneaux/`
- ❌ Claude **CANNOT inspect** the font WOFF2 files in `fonts/`
- ❌ Claude **CANNOT read** the full `panneaux_data.js` file

### If Claude Says "File Not Found"

If Claude responds with "I can't see that file" or "file not found", it's likely because:
1. The file is in `quizz/panneaux/` — this is expected and normal
2. The file is in `fonts/` — this is expected and normal
3. You need to describe the change differently (e.g., modify the HTML instead of the asset)

**This is not a bug — it's intentional to keep performance fast.**

### When to Update .claudeignore

If you add new large directories or binary files, update `.claudeignore` to exclude them. Never exclude:
- HTML, CSS, JS files (core code)
- CLAUDE.md, STYLE_GUIDE.md (documentation)
- robots.txt, sitemap.xml (SEO config)
- Configuration files and documentation

---

## Owner Preferences

- Basque cultural identity is central to the brand (colors: red, teal/green)
- Clean, modern aesthetic with serif headings for elegance
- Self-hosted assets only — no external CDN dependencies (except PDF.js CDN worker for formules pages)
- SEO-optimized: sitemap auto-generated, canonical URLs, meta tags + JSON-LD schema on every page
- Educational content is a priority (20 blog articles, interactive quiz, eco-driving simulator)
- Mobile-first responsive design
- CNIL/RGPD compliant: tarteaucitron manages all cookie consent (GA loaded only after consent)

---

## Développements futurs

Voir `ROADMAP.md` à la racine du projet pour la liste des idées et fonctionnalités à implémenter.
Ce fichier doit être consulté en début de session avant tout nouveau développement.
